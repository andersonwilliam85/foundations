# L Is Modeled R

Leftover is the rank reading. Cut is the $L$ reading. There is no order field. $L$ is the pairing I spend, and $L$ is modeled $R$. A hole is a leftover the curve still lists, and no sitting $R$ whose projection equals it.

## Prior Problems

Wiles wrote, for Clay:

> $\operatorname{rank}\,E(\mathbb{Q})=\operatorname{ord}_{s=1}L(E,s)$.

They asked whether the rank of $E(\mathbb{Q})$ equals the order of vanishing of $L(E,s)$ at $s=1$.

Some ranks were already known in special families. Analytic continuation of $L$ for modular curves was a later room. The leading coefficient was refined into Sha, regulator, periods, Tamagawa, torsion. Hilbert's tenth is the older hunger for a general method on every equation. Those rooms are the literature's.

$E$ is $Y^2=X^3+AX+B$ plus leftover of $E(\mathbb{Q})$. Produce pairs that leftover with the sitting cuts. thisCurve produces thisLock. Not every official $E$ does. Rank is leftover. Ord $L$ is cut. Inverse has to reconstruct the cut; leftover does not store it. Projection looks up the pairing. $L$ is the pairing I spend.

The prior problem is hearing their sentence and filing two functions that must be proved equal, or filing an order field on the leftover, or filing every official $E$ as thisLock. A hole is a missing $R$.

## On The Leftover

I paired $[1..12]$ with the first twelve primes. That pairing is $L$. Leftover $6$ sat with cut $13$. Inverse of leftover $6$ is that cut. $\mathrm{cl}$ of that cut is leftover $6$. OfficialBsd is leftover and cut of the seating $E$ produces. thisCurve produces thisLock. I solved leftover. Not every official $E$ does.

I dropped the sixth pairing on the produced seating. Twelve became eleven. Leftover $6$ is still owed. OfficialBsdOn fails. That hole is the missing $R$.

Weierstrass, LSeries, torsion of $(0,1)$: furniture.

Their worksheet is furniture.

## The Proof

$E$ is $Y^2=X^3+AX+B$ plus leftover of $E(\mathbb{Q})$. Produce pairs that leftover with the sitting cuts. thisCurve produces thisLock. Not every official $E$ does. Rank is leftover. Ord $L$ is cut. $\mathrm{cl}$ is the projection from cut to leftover. Inverse reconstructs the pairing.

$$
\operatorname{rank}=\operatorname{ord} L.
$$

Drop $(6,13)$ on the produced seating. OfficialBsdOn fails. The hole is the missing $R$.

The written proof is [PROOF.md](PROOF.md).

2026-09-09

— William Christopher Anderson
