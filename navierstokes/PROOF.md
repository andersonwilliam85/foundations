# Navier–Stokes: Written Proof

Fefferman, for Clay:

> we ask for a proof of one of the following four statements.

(A) Every admissible unforced open initial datum has a smooth solution for all time.

(B) The periodic analog of that existence.

(C) There exist initial data and a force, decaying as he requires, for which no solutions exist on whole space for all time.

(D) The periodic analog of that breakdown.

Those sentences are the problem they named.

## The Problem

The sit is leftover, pairing, and drop.

OfficialNS produces leftover. Leftover \(T\) is \(\mathrm{cl}\). No continuation field. The continuation through \(T\) is the pairing. OfficialSmooth is leftover whose continuation-\(R\) sits. OfficialSmooth is not Smooth. The leftover is not Global; the inverse is. (A) and (B) are leftover from rest \(u_0\): open or periodic, unforced, leftover, OfficialSmooth. Not \(\forall\) Place \(\to\) Place. Rest Global Smooth is one datum, not (A). Forced MissingR does not refute (A); it is not Unforced. (C) and (D) are leftover, FeffermanDecay, and MissingR. On \([0, 1)\) jumpForce is \(0\). That decays. The hole is MissingR at \(T\), not a new force.

## 1. OfficialNS Produces Leftover

OfficialNS produces leftover. Leftover \(T\) is \(\mathrm{cl}\): the continuation projected onto \([0, T)\). The seated solution is leftover. No continuation field.

A leftover carries the lifespan. Inverse will have to find the continuation.

## 2. Continuation Is The Pairing

The continuation through \(T\) is the pairing. It is a later sit for all future time, fields stay smooth, agrees on \([0, T)\), and keeps force, viscosity, and domain.

That pairing is not stored on leftover.

## 3. Projection Looks Up The Pairing

\(\mathrm{cl}\) projects a continuation onto \([0, T)\). That projection is leftover. Leftover \(T\) is \(\mathrm{cl}\).

\[
\mathrm{leftover}\,T=\mathrm{cl}.
\]

Rest leftover is leftover of rest at \(T=1\): \(\mathrm{cl}\) of rest onto \([0, 1)\). That leftover keeps rest \(u_0\).

## 4. Inverse Reconstructs The Pairing

\(\mathrm{inverse}\) takes a leftover and reconstructs the continuation. The pairing is not stored on leftover.

\[
\mathrm{inverse} : \mathrm{leftover}\to\mathrm{continuation}.
\]

Inverse reconstructs when it sits.

## 5. OfficialSmooth Is Leftover Whose Continuation Sits

OfficialSmooth is leftover whose continuation-\(R\) sits.

OfficialSmooth is not Smooth. Smooth requires Global. The leftover sits on \([0, T)\). It is not Global. The inverse is.

Rest leftover has that pairing. Inverse reconstructs rest. OfficialSmooth holds on the leftover rest produces.

## 6. (A) And (B) Are Leftover From Rest \(u_0\)

(A) is leftover from rest \(u_0\): open, unforced, leftover, OfficialSmooth.

(B) is leftover from rest \(u_0\): the periodic analog.

Not \(\forall\) Place \(\to\) Place. Rest Global Smooth is one datum. It is not (A).

Forced MissingR is not Unforced. It does not refute (A).

I solved (A) and (B).

## 7. (C) And (D) Are Leftover, Decay, And MissingR

(C) is leftover on \([0, 1)\), open, FeffermanDecay, MissingR. On \([0, 1)\) jumpForce is \(0\). That decays. The hole is MissingR at \(T\), not a new force. Blow-up is MissingR.

(D) is the periodic analog.

I solved (C) and (D).

Euler, viscosity zero, is not this prize. That door is empty.

Their worksheet is furniture.

The Lean file is a check, not the proof.

2026-09-08

— William Christopher Anderson
