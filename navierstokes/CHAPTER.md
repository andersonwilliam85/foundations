# The Continuation Is the Pairing

OfficialNS produces leftover. Leftover $T$ is $\mathrm{cl}$. There is no continuation field. The continuation through $T$ is the pairing. A hole is a leftover the lock still lists, and no sitting $R$ whose projection equals it.

## Prior Problems

Fefferman wrote, for Clay:

> we ask for a proof of one of the following four statements.

(A) Every admissible unforced open initial datum has a smooth solution for all time.

(B) The periodic analog of that existence.

(C) There exist initial data and a force, decaying as he requires, for which no solutions exist on whole space for all time.

(D) The periodic analog of that breakdown.

They asked for a proof of one of those four.

OfficialNS produces leftover. Leftover $T$ is $\mathrm{cl}$. Inverse has to reconstruct the continuation; leftover does not store it. Projection looks up the pairing. That pairing is the continuation.

The prior problem is hearing their four sentences and filing $\forall$ Place $\to$ Place as (A), or filing rest Global Smooth as (A), or filing OfficialSmooth as Smooth, or filing a continuation field on the leftover, or filing a new force as the hole, or filing Forced MissingR as a refute of (A). A hole is a missing $R$. Euler, viscosity zero, is not this prize. That door is empty.

## On The Leftover

(A) and (B) are leftover from rest $`u_0`$: open or periodic, unforced, leftover, OfficialSmooth. Rest leftover is $\mathrm{cl}$ of rest onto $[0, 1)$. Inverse reconstructs rest. OfficialSmooth holds on that leftover. OfficialSmooth is not Smooth. The leftover is not Global; the inverse is. Rest Global Smooth is one datum. It is not (A). Not $\forall$ Place $\to$ Place. Forced MissingR does not refute (A). It is not Unforced.

(C) and (D) are leftover, FeffermanDecay, and MissingR. On $[0, 1)$ jumpForce is $0$. That decays. The hole is MissingR at $T$, not a new force.

I solved it.

Their worksheet is furniture.

## The Proof

OfficialNS produces leftover. Leftover $T$ is $\mathrm{cl}$. The continuation through $T$ is the pairing. No continuation field. Inverse reconstructs the pairing when it sits. OfficialSmooth is leftover whose continuation-$R$ sits. OfficialSmooth is not Smooth. The leftover is not Global; the inverse is.

(A) and (B) are leftover from rest $`u_0`$, OfficialSmooth. Not $\forall$ Place $\to$ Place. Rest Global Smooth is one datum, not (A). Forced MissingR does not refute (A).

(C) and (D) are leftover, FeffermanDecay, and MissingR. On $[0, 1)$ jumpForce is $0$. That decays. The hole is MissingR at $T$, not a new force.

The written proof is [PROOF.md](PROOF.md).

2026-09-08

— William Christopher Anderson
